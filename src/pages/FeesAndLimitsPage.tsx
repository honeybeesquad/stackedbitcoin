import LegalPageLayout from "@/components/LegalPageLayout";
import feesAndLimitsHtml from "@/content/legal/fees-and-limits.html?raw";

const FeesAndLimitsPage = () => {
  return <LegalPageLayout html={feesAndLimitsHtml} />;
};

export default FeesAndLimitsPage;
